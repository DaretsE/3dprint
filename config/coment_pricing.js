// ═══════════════════════════════════════════════════════════════════════════
// 📋 КОНФИГУРАЦИОННЫЙ ФАЙЛ КАЛЬКУЛЯТОРА 3D-ПЕЧАТИ
// ═══════════════════════════════════════════════════════════════════════════
// 
// Этот файл содержит все настройки для расчёта стоимости 3D-печати.
// Изменяйте значения ниже под свой бизнес. Все цены в рублях (₽).
//
// ═══════════════════════════════════════════════════════════════════════════

export default {
  
  // ─────────────────────────────────────────────────────────────────────────
  // 🛠️ РАЗДЕЛ 1: НАСТРОЙКИ КАЛЬКУЛЯТОРА
  // ─────────────────────────────────────────────────────────────────────────
  // Эти настройки влияют только на отображение, не на расчёт цены
  
  "calculatorSettings": {
    "defaultCurrency": "₽",        // Валюта: "₽", "$", "€", "₸" и т.д.
    "decimalPlaces": 2             // Знаков после запятой в ценах (2 = 100.50)
  },
  
  // ─────────────────────────────────────────────────────────────────────────
  // 📦 РАЗДЕЛ 1.1: НАДБАВКА К РАСХОДУ МАТЕРИАЛА
  // ─────────────────────────────────────────────────────────────────────────
  // Позволяет автоматически добавлять процент к указанному пользователем
  // расходу материала. Используется для учёта брака, поддержек, промывки
  // смолы и других технологических потерь.
  //
  // КАК РАБОТАЕТ:
  //   Если клиент вводит 100г, а fdmPercent = 15, то расчёт ведётся как 115г.
  //   Клиент видит в поле «Расход материала» надпись (+15%) — если включено.
  //   Если значение 0 — надбавка не применяется, поведение без изменений.
  //
  // НАСТРОЙКИ:

  "materialOverhead": {
    // Процент надбавки для FDM печати (пластик).
    // 0 = надбавка отключена (расход считается ровно как указал клиент).
    // 15 = к расходу клиента автоматически прибавляется 15%.
    "fdmPercent": 0,

    // Процент надбавки для фотополимерной печати (смола).
    // Для смолы обычно выше из-за потерь при промывке и поддержках.
    "resinPercent": 0,

    // Отображение надбавки в интерфейсе рядом с полем "Расход материала":
    //   0 = скобки с процентом НЕ показываются (клиент не видит надбавку)
    //   1 = рядом с названием поля отображается текст "+N%" в скобках
    //
    // Примеры:
    //   showInInterface: 0 → «📦 Расход материала»
    //   showInInterface: 1 → «📦 Расход материала (+15%)»
    //
    // Если fdmPercent и resinPercent оба равны 0 — подпись не показывается
    // даже при showInInterface: 1 (нет смысла показывать «+0%»).
    "showInInterface": 0
  },
  
  // ─────────────────────────────────────────────────────────────────────────
  // 🖨️ РАЗДЕЛ 2: ВАША 3D-ФЕРМА
  // ─────────────────────────────────────────────────────────────────────────
  // Влияет на распределение постоянных затрат (аренда, свет помещения)
  
  "printerFleet": {
    // Количество FDM принтеров в вашей ферме.
    // Чем больше принтеров, тем меньше доля постоянных затрат на каждый заказ.
    "fdmPrinterCount": 5,
    
    // Сколько часов в сутки работают принтеры (для расчёта загрузки).
    // Рекомендуемое значение: 20 часов (остальное время на обслуживание).
    "targetDailyOperatingHours": 20
  },
  
  // ─────────────────────────────────────────────────────────────────────────
  // 💰 РАЗДЕЛ 3: МИНИМАЛЬНАЯ СТОИМОСТЬ ЗАКАЗА
  // ─────────────────────────────────────────────────────────────────────────
  // Если расчётная цена ниже этих значений, показывается уведомление клиенту.
  // Это НЕ меняет расчёт, а только информирует о минимальном пороге заказа.
  
  "minimumOrder": {
    // Минимальная сумма для FDM печати (пластик)
    "fdm": 800,
    
    // Минимальная сумма для фотополимерной печати (смола)
    "resin": 1000
  },
  
  // ─────────────────────────────────────────────────────────────────────────
  // 🏷️ РАЗДЕЛ 4: СКИДКИ ЗА ТИРАЖ (ОПТ)
  // ─────────────────────────────────────────────────────────────────────────
  // Настройте диапазоны количества и проценты скидок.
  // Скидка применяется ко всей сумме заказа.
  
  "discountTiers": [
    // Диапазон 1: от 1 до 19 штук — скидка 0% (нет скидки)
    { "min": 1, "max": 19, "discount": 0 },
    
    // Диапазон 2: от 20 до 49 штук — скидка 10%
    { "min": 20, "max": 49, "discount": 10 },
    
    // Диапазон 3: от 50 до 99 штук — скидка 20%
    { "min": 50, "max": 99, "discount": 20 },
    
    // Диапазон 4: от 100 штук и выше — скидка 30%
    // (max не указан = без верхнего предела)
    { "min": 100, "discount": 30 }
  ],
  
  // ─────────────────────────────────────────────────────────────────────────
  // 🧪 РАЗДЕЛ 5: ЦЕНЫ НА МАТЕРИАЛЫ
  // ─────────────────────────────────────────────────────────────────────────
  
  "costComponents": {
    "materials": {
      
      // ─────────────────────────────────────────────────────────────────────
      // 5.1 ПЛАСТИК ДЛЯ FDM ПЕЧАТИ
      // ─────────────────────────────────────────────────────────────────────
      // pricePerKg — цена за 1 килограмм в рублях
      // name — отображаемое название в выпадающем списке
      // Можно добавлять новые материалы, копируя структуру
      
      "plastic": {
        "pla": {
          "name": "PLA",              // Название в выпадающем списке
          "pricePerKg": 2500,         // Цена за 1 кг (₽)
          "hidden": 0                 // 0 = показывать, 1 = скрыть из списка
        },
        "petg": {
          "name": "PET-G",
          "pricePerKg": 3200,
          "hidden": 0
        },
        "abs": {
          "name": "ABS",
          "pricePerKg": 2800,
          "hidden": 0
        }
        // ─────────────────────────────────────────────────────────────────
        // КАК ДОБАВИТЬ НОВЫЙ ПЛАСТИК:
        //   Скопируйте блок ниже, задайте уникальный ключ, name и pricePerKg.
        //   Параметр hidden обязателен: 0 = показывать, 1 = скрыть.
        // ─────────────────────────────────────────────────────────────────
        // "nylon": {
        //   "name": "Нейлон",
        //   "pricePerKg": 4500,
        //   "hidden": 0
        // },
        // "tpu": {
        //   "name": "TPU (гибкий)",
        //   "pricePerKg": 3800,
        //   "hidden": 0
        // },
        // "asa": {
        //   "name": "ASA",
        //   "pricePerKg": 3000,
        //   "hidden": 0
        // }
      },
      
      // ─────────────────────────────────────────────────────────────────────
      // 5.2 СМОЛА ДЛЯ ФОТОПОЛИМЕРНОЙ ПЕЧАТИ
      // ─────────────────────────────────────────────────────────────────────
      // pricePerLiter — цена за 1 литр в рублях
      
      "resin": {
        "basic": {
          "name": "Базовая смола",
          "pricePerLiter": 5500,
          "hidden": 0                 // 0 = показывать, 1 = скрыть из списка
        },
        "improved": {
          "name": "Улучшенная смола",
          "pricePerLiter": 7500,
          "hidden": 0
        },
        "constructive": {
          "name": "Конструкторская смола",
          "pricePerLiter": 12000,
          "hidden": 0
        }
        // ─────────────────────────────────────────────────────────────────
        // КАК ДОБАВИТЬ НОВУЮ СМОЛУ:
        //   Скопируйте блок ниже, задайте уникальный ключ, name и pricePerLiter.
        //   Параметр hidden обязателен: 0 = показывать, 1 = скрыть.
        // ─────────────────────────────────────────────────────────────────
        // "castable": {
        //   "name": "Литьевая смола",
        //   "pricePerLiter": 15000,
        //   "hidden": 0
        // },
        // "flexible": {
        //   "name": "Гибкая смола",
        //   "pricePerLiter": 9000,
        //   "hidden": 0
        // }
      }
    },
    
    // ─────────────────────────────────────────────────────────────────────
    // 6. СТОИМОСТЬ ВРЕМЕНИ ПЕЧАТИ
    // ─────────────────────────────────────────────────────────────────────
    // Включает: амортизацию, обслуживание, часть электроэнергии, работу оператора
    // costPerMinute — цена за 1 минуту работы принтера
    
    "timeBasedCosts": {
      // FDM принтеры обычно дешевле в эксплуатации
      "fdmPrinter": {
        "costPerMinute": 0.5        // 0.5₽ за минуту = 30₽ за час
      },
      // Фотополимерные принтеры дороже (УФ-лампа, LCD-экран, дороже оборудование)
      "resinPrinter": {
        "costPerMinute": 1.2        // 1.2₽ за минуту = 72₽ за час
      }
    },
    
    // ─────────────────────────────────────────────────────────────────────
    // 7. ОПЕРАЦИОННЫЕ РАСХОДЫ
    // ─────────────────────────────────────────────────────────────────────
    
    "operatingExpenses": {
      
      // 7.1 Потребляемая мощность принтеров (в Ваттах)
      // Укажите реальное потребление ваших принтеров
      "powerConsumption": {
        "fdmWatts": 400,            // FDM принтер: 200-800Вт (зависит от подогрева)
        "resinWatts": 150           // Фотополимер: 100-300Вт (УФ-лампа + экран)
      },
      
      // 7.2 Стоимость электроэнергии
      // Укажите ваш тариф за 1 киловатт-час
      "electricityPricePerKwh": 5.5,  // Пример: Москва 5.5-7₽, регионы 3.5-5.5₽
      
      // 7.3 Амортизация оборудования
      // Рассчитывается как: Цена принтера / Срок службы в часах
      "amortization": {
        "fdmHourlyRate": 50,        // Пример: принтер 50 000₽ / 1000 часов = 50₽/час
        "resinHourlyRate": 150      // Фотополимеры дороже: 150 000₽ / 1000 часов = 150₽/час
      },
      
      // 7.4 Постоянные ежемесячные затраты
      // Распределяются на все заказы пропорционально времени печати
      "fixedMonthlyCosts": {
        "lease": 30000,             // Аренда помещения в месяц (₽)
        "other": 10000              // Другие расходы: интернет, охрана, уборка (₽)
      },
      
      // 7.5 Износ расходников
      // Сопла, трубки, плёнки FEP, перчатки и т.д.
      "consumables": {
        "fdmPerHour": 20,           // FDM: сопла, тефлоновые трубки, ремни (₽/час)
        "resinPerHour": 50          // Фотополимер: FEP-плёнки, фильтры, перчатки (₽/час)
      },
      
      // 7.6 Наценка исполнителя (ваша прибыль)
      // Применяется в конце расчёта ко всей себестоимости
      "operatorMarkupPercent": {
        "fdm": 25,                  // Наценка на FDM печать в процентах (25 = 25%)
        "resin": 35                 // Наценка на фотополимер (обычно выше из-за сложности)
      }
    }
  }
};

// ═══════════════════════════════════════════════════════════════════════════
// 📊 ФОРМУЛА РАСЧЁТА (для понимания как работают настройки)
// ═══════════════════════════════════════════════════════════════════════════
//
// 0. Расход материала с надбавкой = расход × (1 + materialOverhead.fdmPercent / 100)
//    (если надбавка равна 0 — расход не изменяется)
// 1. Стоимость материала = (расход с надбавкой в г или мл / 1000) × цена за кг или литр
// 2. Стоимость времени = время в минутах × costPerMinute
// 3. Электричество = (мощность в Вт × время в часах / 1000) × tariff
// 4. Амортизация = время в часах × amortizationHourlyRate
// 5. Постоянные затраты = время в минутах × (lease + other) / (принтеры × часы × 30 × 60)
// 6. Расходники = время в часах × consumablesPerHour
// 7. Себестоимость = 1 + 2 + 3 + 4 + 5 + 6
// 8. Цена с наценкой = себестоимость × (1 + markup% / 100)
// 9. Общая цена = цена с наценкой × количество
// 10. Скидка = общая цена × discount% / 100
// 11. ИТОГО = общая цена - скидка
//
// ═══════════════════════════════════════════════════════════════════════════
// 💡 СОВЕТЫ ПО НАСТРОЙКЕ
// ═══════════════════════════════════════════════════════════════════════════
//
// 1. Начните с небольших наценок (20-25%), затем увеличивайте по спросу
// 2. Указывайте реальные цены на материалы (проверяйте актуальные)
// 3. Для amortizationHourlyRate: разделите цену принтера на 1000-2000 часов
// 4. Минимальный заказ защитит от убыточных мелких заказов
// 5. Скидки за тираж мотивируют клиентов заказывать больше
//
// ═══════════════════════════════════════════════════════════════════════════
